#include "vertex.h"

//render_point render_point::interpolate(const render_point &a, const render_point &b, float alpha) {
//    render_point result;
//    result.x = a.x * (1 - alpha) + b.x * alpha;
//    result.y = a.y * (1 - alpha) + b.y * alpha;
////    result.z = a.z * (1 - alpha) + b.z * alpha;
//    result.v = a.v * (1 - alpha) + b.v * alpha;
//    result.vn = a.vn * (1 - alpha) + b.vn * alpha;
//    result.vt = a.vt * (1 - alpha) + b.vt * alpha;

//    return result;
//}
